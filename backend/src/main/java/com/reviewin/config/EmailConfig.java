package com.reviewin.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class EmailConfig {
    // JavaMailSender is auto-configured from spring.mail properties in application.yml.
}
